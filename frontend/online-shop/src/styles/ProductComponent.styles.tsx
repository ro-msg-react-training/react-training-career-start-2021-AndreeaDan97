import { makeStyles } from "@material-ui/core";

export const productComponentStyle = makeStyles((theme) => ({
  customCard: {
    borderStyle: "outset",
    borderWidth: 2,
    borderRadius: 15,
  },
}));
